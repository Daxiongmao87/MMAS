from setuptools import setup
setup(
            name='mmas',
            packages=['mmas'],
            include_package_data=True,
            install_requires=[
                'flask',
            ],
        )
